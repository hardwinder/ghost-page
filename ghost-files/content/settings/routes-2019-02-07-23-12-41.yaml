routes:
  /:
    controller: channel
    data: page.my-first-post
    template:
        - post

collections:
  /:
    permalink: /{slug}/
    template:
      - index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
